import { Card, Container } from "react-bootstrap";

const ConfirmationForm: React.FC = () => {
  return (
    <Container>
      <Card className="shadow">
        <h4>ConfirmationForm</h4>
      </Card>
    </Container>
  );
};

export default ConfirmationForm;
